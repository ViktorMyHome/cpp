// steps to follow during zmq lib installation:
// download zmq library from git
// https://github.com/zeromq/libzmq/
// ./autogen.sh
// ./configure --enable-drafts=yes
// make
// sudo make install

// In current configuration one server and two clients are available.
// To lunch each sample go into the directory and type:
// make clean
// make all
// ./main

// Result of work is stored in clients out_file.txt
// Settings are in client_config.txt, server_config.txt
